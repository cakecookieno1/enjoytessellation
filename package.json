{
  "name": "enjoytessellation",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check api/_storage.js && node --check api/auth.js && node --check api/posts.js && node scripts/verify-app.js",
    "serve": "python -m http.server 5173 --bind 127.0.0.1"
  }
}
